import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';
// Remove or comment out the following line if reportWebVitals is not used
// import reportWebVitals from './reportWebVitals';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

// Remove or comment out the following line if reportWebVitals is not used
// reportWebVitals();
